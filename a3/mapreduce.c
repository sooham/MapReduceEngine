#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>
#include <unistd.h>
#include <string.h>


/**
 * Reads the filename of all files in a directory,
 * and sends them evenly to map worker pipes.
 * @param path the path to the directory.
 */
void walk_directory(char *path){
	
}


int main(){

}